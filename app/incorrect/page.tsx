'use client';
import { useEffect, useMemo, useState } from 'react';
import Link from 'next/link';
import { allQuestions } from '../../lib/data';
import { getCurrentUserId, loadAttempts } from '../../lib/persistence';
export default function Incorrect(){const [bad,setBad]=useState<Record<string,{incorrect:number;latest:string}>>({});const [status,setStatus]=useState('Loading attempt history…');useEffect(()=>{(async()=>{const uid=await getCurrentUserId();if(!uid){setStatus('Sign in to access your synchronized attempt history.');return}try{const attempts=await loadAttempts(uid);const map:Record<string,{incorrect:number;latest:string}>={};for(const a of attempts){if(!map[a.question_id]) map[a.question_id]={incorrect:0,latest:a.result};if(a.result==='incorrect') map[a.question_id].incorrect+=1;if(!map[a.question_id].latest) map[a.question_id].latest=a.result;}setBad(Object.fromEntries(Object.entries(map).filter(([,v])=>v.incorrect>0)));setStatus('')}catch{setStatus('Could not load attempt history.')}})()},[]);const rows=useMemo(()=>allQuestions.filter(q=>bad[q.id]),[bad]);return <div className="setup"><div className="page-title"><div><div className="eyebrow">Attempt history</div><h1>Incorrect questions</h1><p>Questions with at least one incorrect attempt. The history is persistent rather than a simple flag.</p></div>{rows.length>0&&<Link className="btn btn-primary" href="/practice?only=incorrect">Practice again</Link>}</div><div className="card section">{rows.length===0?<div className="empty">{status||'No incorrect-question history yet.'}</div>:<div className="table-like"><div className="table-row header"><span>Question</span><span>Incorrect attempts</span><span>Latest result</span><span>Source</span></div>{rows.map(q=><div className="table-row" key={q.id}><span><b>{q.number} · {q.title}</b><div className="muted">{q.subject} · {q.topic}</div></span><span>{bad[q.id].incorrect}</span><span className={bad[q.id].latest==='incorrect'?'danger':'success'}>{bad[q.id].latest}</span><span>{q.gateOverflowUrl&&<a href={q.gateOverflowUrl} target="_blank" rel="noreferrer" className="btn btn-soft">GateOverflow</a>}</span></div>)}</div>}</div></div>}
