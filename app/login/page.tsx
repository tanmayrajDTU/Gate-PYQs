'use client';
import {useState} from 'react';
import {supabase} from '../../lib/supabase';
export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[mode,setMode]=useState<'login'|'signup'|'reset'>('login'),[message,setMessage]=useState('');const submit=async()=>{if(!supabase){setMessage('Supabase is not configured. Add NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY.');return}setMessage('');if(mode==='reset'){const {error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/login?reset=1`});setMessage(error?.message||'Password reset email sent.');return}const {error}=mode==='login'?await supabase.auth.signInWithPassword({email,password}):await supabase.auth.signUp({email,password});if(error)setMessage(error.message);else setMessage(mode==='login'?'Logged in.':'Account created. Check your email if confirmation is enabled.');};return <div className="setup" style={{maxWidth:520}}><div className="page-title"><div><div className="eyebrow">Account</div><h1>{mode==='login'?'Welcome back':mode==='signup'?'Create your account':'Reset your password'}</h1><p>Sync attempts, bookmarks, revision and practice sessions across devices.</p></div></div><div className="card section"><div className="field"><label>Email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></div>{mode!=='reset'&&<div className="field" style={{marginTop:14}}><label>Password</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••"/></div>}<button className="btn btn-primary" style={{width:'100%',justifyContent:'center',marginTop:18}} onClick={submit}>{mode==='login'?'Login':mode==='signup'?'Sign up':'Send reset email'}</button>{message&&<p className="muted" style={{marginTop:14}}>{message}</p>}<div style={{marginTop:18,textAlign:'center',display:'flex',gap:8,justifyContent:'center',flexWrap:'wrap'}}>{mode!=='reset'&&<button className="btn" style={{background:'transparent',color:'var(--accent)'}} onClick={()=>setMode(mode==='login'?'signup':'login')}>{mode==='login'?'Create an account':'Already have an account? Login'}</button>}{mode==='login'&&<button className="btn" style={{background:'transparent',color:'var(--accent)'}} onClick={()=>setMode('reset')}>Forgot password?</button>}{mode==='reset'&&<button className="btn" style={{background:'transparent',color:'var(--accent)'}} onClick={()=>setMode('login')}>Back to login</button>}</div></div></div>}
